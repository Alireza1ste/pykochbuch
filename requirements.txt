# --- Core Dependencies ---
# (None required as they are built into Python 3.9+)

# --- Development & Testing ---
pytest>=7.0.0          # For running your recipe logic tests
pytest-mock>=3.10.0     # If you ever need to mock database calls

# --- Packaging Tools ---
setuptools>=61.0        # Required for the build system
build>=1.0.0           # Required to generate the .whl file
wheel>=0.40.0          # Required to support the wheel format